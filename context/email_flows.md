# Email flows

Document Firebase SendGrid mail extension behavior here.

## Email provider
- Firebase SendGrid mail extension

## Trigger collection/path
TODO: Example: `mail`, `mails`, or another configured collection.

## Document shape
TODO: Document the exact fields expected by the extension.

Example:
```json
{
  "to": ["user@example.com"],
  "message": {
    "subject": "Subject",
    "text": "Plain text",
    "html": "<p>HTML</p>"
  }
}
